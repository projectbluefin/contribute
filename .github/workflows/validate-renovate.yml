name: Validate Renovate Config
# Thin caller -- logic lives in projectbluefin/actions/reusable-validate-renovate.yml

on:
  pull_request:
    paths:
      - "renovate.json"
  push:
    branches: [main]
    paths:
      - "renovate.json"

permissions: {}

jobs:
  validate:
    permissions:
      contents: read
    uses: projectbluefin/actions/.github/workflows/reusable-validate-renovate.yml@9e1acbe66ec87cd07b573e1671a3ecaee7df73d0 # v1
