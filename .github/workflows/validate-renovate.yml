name: Validate Renovate Config
# Thin caller -- logic lives in projectbluefin/actions/reusable-validate-renovate.yml

on:
  pull_request:
    paths:
      - "renovate.json"
  push:
    branches: [main]
    paths:
      - "renovate.json"

permissions: {}

jobs:
  validate:
    permissions:
      contents: read
    uses: projectbluefin/actions/.github/workflows/reusable-validate-renovate.yml@722bdf2e7fda6d5fac91c78c6ed396d7cf9a8dc5 # v1
