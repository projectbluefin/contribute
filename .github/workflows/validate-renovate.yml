name: Validate Renovate Config
# Thin caller -- logic lives in projectbluefin/actions/reusable-validate-renovate.yml

on:
  pull_request:
    paths:
      - "renovate.json"
  push:
    branches: [main]
    paths:
      - "renovate.json"

permissions: {}

jobs:
  validate:
    permissions:
      contents: read
    uses: projectbluefin/actions/.github/workflows/reusable-validate-renovate.yml@e461ac9622be50a7504c41da4b666fc3c1e47770 # v1
