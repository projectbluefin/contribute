# Replace the placeholder before applying. Keep the real contributor token out
# of source control.
apiVersion: v1
kind: Secret
metadata:
  name: contribute-secret
  namespace: bluefin-system
  labels:
    app.kubernetes.io/name: contribute
    app.kubernetes.io/component: contributor
    app.kubernetes.io/part-of: review
type: Opaque
stringData:
  contributor.env: |
    HIVE_REGISTRATION_TOKEN=replace-with-real-token
    HIVE_HUB=wss://hosted-projectbluefin-knuckle-gjvq.hive.hivecommons.dev/contribute
    CONTRIBUTOR_ID=contributor-1
    CONTRIBUTOR_USERNAME=castrojo
    AGENT_BACKEND=omp
  GH_TOKEN: replace-with-gh-token
  GITHUB_COPILOT_TOKEN: replace-with-copilot-token
